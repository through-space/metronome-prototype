import { DEFAULT_TEMPO } from "@config/metronomeConfig";
export var SET_TEMPO_DEBOUNCE_TIMEOUT = 200;
export var DEFAULT_TIMER_CONTEXT = {
    tempo: DEFAULT_TEMPO,
    metronomeStateMachine: null,
};
